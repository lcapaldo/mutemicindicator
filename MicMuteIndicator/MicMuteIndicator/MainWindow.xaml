<Window x:Class="MicMuteIndicator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MicMuteIndicator"
        mc:Ignorable="d"
        Title="Microphone Light" Height="350" Width="525" Name="theMainWindow">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <Style x:Key="LiveMicLabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Impact" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Red" />
            <Setter Property="FontSize" Value="48" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Label Style="{StaticResource LiveMicLabelStyle}" Visibility="{Binding Path=MicIsLive, ElementName=theMainWindow, Converter={StaticResource BoolToVis}}">LIVE MIC</Label>
</Window>
